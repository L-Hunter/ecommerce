<!DOCTYPE html>
<html>
  <head>
    <title>Sundry Detritus Emporium</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
      <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

  </head>

  <body>
  <div class="container-fluid">

  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <h1 class="text-center">Sundry Detritus Emporium</h1>
      <div class="row">
      
        <div class="col-sm-6 text-left">
          <%= form_tag(products_path, :method => "get", id: "search-form") do %>
          <%= text_field_tag :search, params[:search], placeholder: "Search Products" %>
          <%= submit_tag "Search", :name => nil %>
          <% end %>
        </div>

        <div class="col-sm-3">
          <a href="/products">Our Products</a>
        </div>

        <div class="col-sm-3">
          <% if !current_user %>
            <a href="/users">Log in or Sign up</a>
           <% end %> 

          <% if current_user %>
      	     <a href="/users/<%= "#{session[:user_id]}" %> ">Your Account</a>
          <% end %>
        </div>

      </div>
    </div>
  </nav>

    <div id="wrapper"> 
      <% if !current_user %>
        <h2 class="text-center">Log in to Start Shopping</h2>
      <% end %> 

      <%= yield %>

    </div>
  </div>
  </body>
</html>
